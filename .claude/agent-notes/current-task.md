# Текущий статус

## v1 — ГОТОВ (2026-05-12)

Сделано: рабочий Next.js проект, дашборд v1 (тикеры + главный equity-график + карточки участников, тёмная тема), плейсхолдер-данные в `data/competition.ts`.

Состояние git: закоммичено локально (`feat: initial dashboard v1 — equity chart + tickers + participant cards`), **НЕ запушено** — пользователь сам запушит после проверки. Vercel-деплоя нет.

Билд: `npm run build` чисто, `npm run lint` чисто, `npm run dev` → 200.

## Что дальше — v2

По спеку из `~/.claude/agents/dima.md`:
- **Правые карточки участников** с полными таблицами открытых позиций:
  - SIDE (LONG/SHORT), инструмент, размер лота, «Объём», «План выхода» (TP/SL), «Нереализованный PnL»
  - «Свободные средства» внизу карточки
  - модель `Position` уже есть в `data/competition.ts`, `availableCash` тоже — нужно заполнить и отрисовать
- **Табы** (вверху справа): «Закрытые сделки» / «Список наблюдения» / «Открытые позиции» / «О соревновании»
- **Фильтр** «Все участники» — дропдаун, показать всех / выбрать конкретных (повлияет на линии графика и карточки)
- Возможно: разнести страницу на server-shell + client-интерактивная часть (фильтр требует состояния)

## v3 (потом)
- Анимации линий, экспорт графика (PNG), лидерборд с медальками, страница `/admin` для редактирования

## Открытые вопросы к пользователю
1. Реальные имена участников + стартовые депозиты (сейчас плейсхолдеры)
2. Реальное название соревнования и точные даты (сейчас «Реалити-торговля: 2 недели», 13–27 мая)
3. Точный список тикеров для верхней строки (сейчас XAUUSD/XAGUSD/EURUSD/GBPUSD/USDJPY/BTCUSD)
4. Нужны ли реальные фото-аватары участников (сейчас инициалы) — если да, класть в `/public` и прописывать путь в `participant.avatar`
5. Нужна ли страница `/admin` или ручное редактирование файла достаточно (пока — ручное)
