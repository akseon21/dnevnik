-- ─────────────────────────────────────────────────────────────────────────────
-- v10.2 — Обновление подписи под графиком в competition_meta
--
-- Убирает хвост «· live-PnL из TwelveData по цене входа» из note.
-- В UI это выводится как: «📊 ${note} · до следующего обновления: M:SS».
-- Хвост про TwelveData дублировал техническую деталь и захламлял подпись.
--
-- Идемпотентно: можно прогонять много раз, повторный прогон = no-op.
-- WHERE-условие через `IS DISTINCT FROM` корректно сравнивает и NULL.
-- ─────────────────────────────────────────────────────────────────────────────

update public.competition_meta
   set note = 'Баланс и equity считаются автоматически из сделок'
 where note is distinct from 'Баланс и equity считаются автоматически из сделок';
